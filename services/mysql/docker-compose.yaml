version: '3.3'
services:
  mysql:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 'test'
    # Where our data will be persisted
    volumes:
      - mysql:/var/lib/mysql
# Names our volume
volumes:
  mysql:

